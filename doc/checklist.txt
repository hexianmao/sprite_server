1.客户端（A,B）与服务器端建立TCP连接。
2.客户端A通过TCP向服务器端请求呼叫客户端B。
3.服务器端收到A的呼叫请求后，检查B是否在线，然后通过TCP通知B有A的请求过来，并告诉B，A的地址和端口。
4.客户端B收到服务器端的通知后，根据服务器提供的A的地址和端口试图用UDP联系A，然后B告诉服务器端接受A的呼叫请求。
5.服务器端收到B的接受呼叫请求消息后，通过TCP通知A，B已接受请求并告知B的地址和端口。
6.客户端A在得到服务器端的消息后，根据服务器端提供的B的地址和端口通过UDP联系B。
7.此时，A通过UDP成功连接到B，双方可进行实时的语音传输。

